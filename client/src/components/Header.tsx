import React from 'react';

interface HeaderProps {
  toggleMobileMenu: () => void;
}

// Header component is empty as per user request (no "Neil Bhammar" header)
const Header: React.FC<HeaderProps> = () => {
  return null;
};

export default Header;
